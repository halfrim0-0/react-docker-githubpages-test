import { themeColor } from "./common";

export const menuWidth = "w-1/2";
export const menuHeight = "py-5";
export const menuBorder = `border border-${themeColor}-100`;
export const menuTextColor = "text-white";
export const menuBackgroundColor = `bg-${themeColor}-400 hover:bg-${themeColor}-300`;
