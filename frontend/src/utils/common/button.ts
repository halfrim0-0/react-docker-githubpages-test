const normalButtonColor: string = "bg-blue-400";
const normalButtonTextColor: string = "text-white";
const normalButtonWidth: string = "px-2";
const normalButtonShape: string = "rounded";
export const normalButton: string = `${normalButtonColor} ${normalButtonTextColor} ${normalButtonWidth} ${normalButtonShape}`;
